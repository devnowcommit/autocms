seo = new FlowRouterSEO({
  database: true,
	// databaseName: 'seo'
});
/*
settingValues = settings.find({}, {limit:1}).fetch()[0];

seo.setDefaults({
  title: settingValues.title,
  description: settingValues.description
});

/*
seo.set({
  title: settings.title,
	description: settings.description
});
*/